/**
 * @deprecated Category aliases are no longer needed — products have correct
 * category_id values from the official ALDI data import.
 */

export const SEED_CATEGORY_ALIAS_ENTRIES: {
  term_normalized: string;
  category_id: string;
}[] = [];
